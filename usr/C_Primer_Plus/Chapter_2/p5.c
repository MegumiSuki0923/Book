#include <stdio.h>

int main(void)
{
        int toes;
        toes = 10;

        printf("toes is %d\n", toes);
        printf("%d + %d is %d\n", toes, toes, toes + toes);
        printf("%d * %d is %d\n", toes, toes, toes * toes);

        return 0;
}