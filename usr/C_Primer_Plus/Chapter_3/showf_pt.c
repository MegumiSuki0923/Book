#include <stdio.h>

int main(void)
{
        float abort = 32000.0;
        double abet = 2.14e9;

        printf("%f can be writen %e.\n", abort, abort);
        printf("%f can be writen %e.\n", abet, abet);

        return 0;
}