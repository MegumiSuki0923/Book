#include <stdio.h>

#define LEN	30

enum spectrum {
	red, orange, yellow, green, blue, violet
};

int main(void)
{
	char choice[LEN];
	int color;

	puts("Enter a color (empty line to quit):");
	while(gets(choice) && choice[0] != '\0')
	{
		for(co)
	}

	return 0;
}
